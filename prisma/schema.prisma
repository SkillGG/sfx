// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider  = "postgresql"
    url       = env("DATABASE_URL")
    directUrl = env("DIRECT_URL")
}

model SFX {
    id Int @id @default(autoincrement())

    text String
    read String
    def  String

    extra String?

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    ogTranslations Translation[] @relation("ogSFX")
    tlTranslations Translation[] @relation("tlSFX")

    @@index([text])
    @@index([read])
}

model Translation {
    id Int @id @default(autoincrement())

    sfx1Id Int
    ogSFX  SFX @relation("ogSFX", fields: [sfx1Id], references: [id])

    sfx2Id Int
    tlSFX  SFX @relation("tlSFX", fields: [sfx2Id], references: [id])

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    additionalInfo String?

    @@index([sfx1Id])
    @@index([sfx2Id])
}

model UserSession {
    id Int @id @default(autoincrement())

    createdAt DateTime @default(now())
    maxTime   Int      @default(1800000) // 30 mins

    token String @unique
}
